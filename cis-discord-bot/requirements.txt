# Discord.py - Discord API wrapper
discord.py==2.3.2

# Anthropic Claude SDK
anthropic==0.18.0

# Environment variables
python-dotenv==1.0.0

# Async HTTP (required by discord.py)
aiohttp==3.9.1
httpx==0.27.2
pytz==2024.1

# Database — PostgreSQL (production: Railway)
psycopg2-binary==2.9.9
# asyncpg reserved for future full-async migration
asyncpg==0.29.0
# sqlite3 is stdlib (Python 3.10+) — retained for local test environment only

# Testing (optional, for development)
pytest==7.4.3
pytest-asyncio==0.21.1
