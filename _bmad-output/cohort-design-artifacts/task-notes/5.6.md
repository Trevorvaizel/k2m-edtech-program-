# Task 5.6: Create server template for future cohorts

**Task:** 5.6 - Create server template for future cohorts  
**Status:** COMPLETE  
**Date:** 2026-02-20  
**Developer:** Dev Agent (Amelia)

---

## Scope Completed

1. Confirmed template artifact exists and is generated by automation:
   - `setup_discord_server.py` -> `create_server_template(guild)`
   - Existing template code: `Eqz6DMNHuVHU`
2. Authored a playbook-aligned deployment runbook:
   - `_bmad-output/cohort-design-artifacts/cohort-template-deployment-checklist.md`
   - Includes required Story 5.1 checklist items:
     - rename server
     - update cohort dates
     - create new cohort DB
     - update env vars
     - test bot connectivity
     - invite Trevor and generate student invite
3. Added missing `COHORT_ID` to template environment config for explicit cohort isolation:
   - `cis-discord-bot/.env.template`
4. Verified runbook timing stays within the playbook target:
   - `~30 minutes` with template path

---

## Playbook / Spec Anchors Used

1. `_bmad-output/cohort-design-artifacts/discord-implementation-sprint.yaml` (Task 5.6 description + acceptance)
2. `_bmad-output/cohort-design-artifacts/playbook-v2/05-discord-ops/5-1-discord-server-structure.md`
   - `Copy-Paste Server Template`
   - `Deployment Checklist (Per New Cohort)`
   - `Time to Deploy: ~30 minutes (with template)`
3. `_bmad-output/cohort-design-artifacts/task-notes/0.5.md` (existing template artifact evidence)

---

## Files Updated

1. `_bmad-output/cohort-design-artifacts/cohort-template-deployment-checklist.md`
2. `cis-discord-bot/.env.template`
3. `_bmad-output/cohort-design-artifacts/discord-implementation-sprint.yaml`
4. `_bmad-output/cohort-design-artifacts/task-notes/README.md`
5. `_bmad-output/cohort-design-artifacts/task-notes/5.6.md`

---

## Verification

Checklist/spec alignment checks:

```bash
Select-String -Path setup_discord_server.py -Pattern "create_server_template","K2M Cohort Template"
Select-String -Path cis-discord-bot/.env.template -Pattern "^COHORT_ID=","^COHORT_START_DATE="
Select-String -Path _bmad-output/cohort-design-artifacts/cohort-template-deployment-checklist.md -Pattern "Copy server template","COHORT_ID","Test bot connectivity","30 minutes"
```

Result: required template/checklist/env anchors are present and mapped to Story 5.1 Task 5.6 acceptance criteria.

---

## Notes

- Task 5.6 is operations/documentation driven; no runtime bot logic changes were required.
- Per-cohort execution still requires manual Discord actions (template copy, invite generation, announcements), now captured in one canonical runbook.
