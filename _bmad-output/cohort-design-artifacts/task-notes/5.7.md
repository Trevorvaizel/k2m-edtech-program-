# Task 5.7: Production Deployment + Go-Live Checklist

**Task:** 5.7 - Production deployment + go-live checklist  
**Status:** COMPLETE  
**Date:** 2026-02-20  
**Developer:** Dev Agent (Amelia)

---

## Scope Completed

1. Created and finalized the playbook-grounded go-live runbook:
   - `_bmad-output/cohort-design-artifacts/operations/checklists/cohort-production-go-live-checklist.md`
2. Provisioned Railway production:
   - Project: `k2m-cis-discord-bot`
   - Service: `kira-bot`
   - Persistent volume mounted at `/data`
3. Synced production environment variables and runtime commands in Railway:
   - `BUILD_COMMAND=pip install -r requirements.txt`
   - `START_COMMAND=python main.py`
   - `DATABASE_PATH=/data/cohort-1.db`
   - Discord + provider + channel + cohort variables
4. Closed production runtime blockers discovered during deployment:
   - Forced Railpack runtime from Python 3.13 to 3.12 via `RAILPACK_PYTHON_VERSION=3.12` (fixes `audioop` removal issue).
   - Added missing dependency `pytz` to `requirements.txt` for scheduler/escalation/participation modules.
   - Made daily prompt library deploy-safe by adding local fallback loading and bundling the prompt library in bot package.
5. Generated live student invite and posted go-live announcement in `#announcements` with exact date.
6. Recorded credential handoff and marked go/no-go decision.

---

## Playbook / Spec Anchors Used

1. `_bmad-output/cohort-design-artifacts/operations/sprint/discord-implementation-sprint.yaml` (Task 5.7 description + acceptance)
2. `_bmad-output/cohort-design-artifacts/playbook-v2/05-discord-ops/5-1-discord-server-structure.md`
   - `Deployment Checklist (Per New Cohort)`
   - `Server Handoff to Trevor`
3. `_bmad-output/cohort-design-artifacts/playbook-v2/05-discord-ops/5-6-manual-sops.md`
   - Daily/weekly monitoring + escalation + crisis operations
4. `_bmad-output/cohort-design-artifacts/playbook-v2/04-cis-agents/4-7-discord-bot-spec.md`
   - Railway deployment flow (`init`, `add`, `up`, runtime vars)

---

## Acceptance Gate Status

All Task 5.7 gates are now `READY`:

- Bot deployed to Railway production: **READY**
- Environment variables set: **READY**
- Database initialized on production path (`/data/cohort-1.db`): **READY**
- Monitoring dashboard + health path accessible: **READY**
- Trevor credential handoff block completed: **READY**
- Student invite link generated: **READY**
- `#announcements` updated with cohort start date: **READY**
- SOP handoff coverage in place: **READY**
- Go/no-go decision: **GO**

---

## Verification

Railway verification:

```bash
railway whoami
railway status
railway service status
railway logs <deployment-id> --lines 120
```

Result: Production project linked, service deployment `SUCCESS`, bot connected to Discord, scheduler/health/escalation/participation subsystems initialized, daily backups active.

Runtime health:

```bash
python check_discord_health.py
```

Result: **PASS** (`10 passes`, `0 warnings`, `0 failures`).

Targeted regression checks for production fixes:

```bash
pytest -q tests/test_scheduler.py tests/test_week1_journey_e2e.py
pytest -q tests/test_scheduler.py tests/test_escalation_system.py tests/test_cluster_session_automation.py
pytest -q tests/test_performance_cost_validation_e2e.py tests/test_health_monitor.py
```

Result:
- `30 passed, 1 warning`
- `58 passed, 1 warning`
- `38 passed, 1 warning`

Go-live actions verified:
- Student invite generated: `https://discord.gg/eMFecMq2Vu`
- Go-live announcement posted in announcements channel (`message_id=1474366903098282090`)

---

## Files Updated

1. `_bmad-output/cohort-design-artifacts/operations/sprint/discord-implementation-sprint.yaml`
2. `_bmad-output/cohort-design-artifacts/operations/checklists/cohort-production-go-live-checklist.md`
3. `_bmad-output/cohort-design-artifacts/task-notes/README.md`
4. `_bmad-output/cohort-design-artifacts/task-notes/5.7.md`
5. `cis-discord-bot/requirements.txt`
6. `cis-discord-bot/scheduler/daily_prompts.py`
7. `cis-discord-bot/scheduler/daily-prompt-library.md`

---

## Notes

- Railway variable listings were handled without exposing secrets in artifacts; credential references remain masked in runbooks.
- Voice support warning (`PyNaCl not installed`) remains non-blocking for this cohort flow.
