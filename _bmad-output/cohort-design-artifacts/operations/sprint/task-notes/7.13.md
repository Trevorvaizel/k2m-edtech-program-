# Task 7.13: Brevo Account Setup + Domain Authentication (DKIM/SPF)

**Date:** 2026-03-11  
**Status:** done (infrastructure complete; technical debt logged: `TD-7.13-01`)  
**Scope:** Decision N-13 + N-24 (Session 4)

---

## Context Read and Contract Alignment

Loaded and executed against:

- `_bmad-output/cohort-design-artifacts/design-and-architecture/onboarding-context-canonical-v3.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/student-onboarding-and-enrollment-flow.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/context-engine-experience-design.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/discord-community-culture-and-architecture.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/05-discord-ops/discord-architecture.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/01-philosophy/guardrails-preserved.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/pre-mortem-session-4-decisions.md`
- `_bmad-output/cohort-design-artifacts/operations/sprint/discord-implementation-sprint.yaml`

Execution respected the canonical sequencing (`7.12 -> 7.13 -> 7.14`) and pre-join comms policy (Brevo-first, no KIRA pre-join DMs).

---

## What Was Executed

### 1) Brevo domain + sender state

Live Brevo API checks and setup actions were run from this workspace against the target sender/domain:

- Account API reachable (`/account`, 200)
- Domain detail fetched (`GET /senders/domains/k2mlabs.com`, 200)
- Sender lookup resolved `Trevor from K2M <trevor@k2mlabs.com>` (active=true on final run)
- Final test send probe tracked in Brevo events (`requests` then `delivered`)

### 2) Repeatable audit tooling added

New script:

- `cis-discord-bot/scripts/task-713-brevo-audit.py`

Capabilities:

- read-only audit (default)
- optional mutation flags (`--ensure-domain`, `--ensure-sender`)
- optional transactional probe (`--send-test`)
- JSON evidence output for sprint notes

Run performed:

```bash
python cis-discord-bot/scripts/task-713-brevo-audit.py --sender-email trevor@k2mlabs.com --domain k2mlabs.com --send-test --test-to k2m.labs@gmail.com --wait-seconds 45 --output _bmad-output/cohort-design-artifacts/operations/sprint/task-notes/7.13-brevo-audit-k2mlabs.json
```

Evidence artifact:

- `_bmad-output/cohort-design-artifacts/operations/sprint/task-notes/7.13-brevo-audit-k2mlabs.json`
- `_bmad-output/cohort-design-artifacts/operations/sprint/task-notes/7.13-live-evidence.txt`

### 3) Runtime docs/config alignment

Updated:

- `cis-discord-bot/README.md` (task 7.13 audit command + expected ready-state)
- `cis-discord-bot/.env.template` (target sender values moved to `trevor@k2mlabs.com`)
- `cis-discord-bot/scripts/task-713-brevo-audit.py` defaults switched to `k2mlabs.com` sender flow

### 4) Email #1 screenshot asset prep (N-24 dependency)

Added capture brief:

- `cis-discord-bot/assets/email/email-1/discord-orientation-asset-brief.md`

This defines required annotations and final asset path for Brevo template embedding.

---

## Live Findings

From Vercel DNS + Brevo domain audit:

- `k2mlabs.com` DNS records now resolve publicly and report `status=true` in Brevo:
  - `brevo1._domainkey` (CNAME) -> `b1.k2mlabs-com.dkim.brevo.com`
  - `brevo2._domainkey` (CNAME) -> `b2.k2mlabs-com.dkim.brevo.com`
  - `@` TXT includes `brevo-code:a8500676b5dc8cf194ea2e605f995050`
  - `_dmarc` TXT set to `v=DMARC1; p=none; rua=mailto:rua@dmarc.brevo.com`
- Brevo domain now reports:
  - `verified=true`
  - `authenticated=true`

From sender audit:

- `trevor@k2mlabs.com` exists in Brevo and is **active**

From runtime env audit:

- Local `.env` runtime used for direct script runs targets `trevor@k2mlabs.com`
- Railway production sender values were confirmed:
  - `EMAIL_FROM=trevor@k2mlabs.com`
  - `PARENT_EMAIL_FROM=trevor@k2mlabs.com`
  - `PARENT_EMAIL_REPLY_TO=trevor@k2mlabs.com`
  - `BREVO_API_KEY` present
  - `CHANNEL_WELCOME_LOUNGE=1481153734363840613` now set in production

From test-send probes:

- Brevo accepts request creation (`POST /smtp/email` -> 201) and reports `requests` + `delivered` events.

From production runtime probes:

- `GET https://kira-bot-production.up.railway.app/health` returned `200` with `status=healthy`, `db=connected`, `discord=connected`.
- Discord API health check passed with zero failures using current production IDs (artifact: `task-notes/7.13-discord-health-production.json`).

Deferred item:

- `cis-discord-bot/assets/email/email-1/discord-orientation-annotated.png` is still missing and is deferred as technical debt `TD-7.13-01`.

---

## Acceptance Check

- [x] DKIM/SPF records verified in Brevo (`Authenticated` true)
- [x] Test email from `trevor@k2mlabs.com` to Gmail is delivered (`requests` + `delivered` events captured)
- [x] `BREVO_API_KEY` exists in Railway production runtime
- [x] Railway production `EMAIL_FROM` switched to `Trevor from K2M <trevor@k2mlabs.com>`
- [ ] Gmail inbox vs spam-folder placement evidence is not machine-verifiable here; manual classification check is deferred to task 7.15 deliverability sweep
- [ ] Annotated Discord screenshot file produced and ready for Email #1 template
- [x] Screenshot gap deferred and tracked as technical debt `TD-7.13-01` with explicit gate in task 7.14

Task 7.13 is closed as infrastructure-complete with one deferred manual asset item; inbox placement validation is explicitly handled in task 7.15.

---

## Required Manual Actions (Trevor)

1. Capture and save `cis-discord-bot/assets/email/email-1/discord-orientation-annotated.png` per the asset brief.
2. Ensure this asset is embedded before task 7.14 (Email templates) is marked done.
3. Re-run the audit command below only if sender/domain state changes again.

---

## Technical Debt Deferral

- Debt ID: `TD-7.13-01`
- Register: `_bmad-output/cohort-design-artifacts/operations/sprint/technical-debt-register.md`
- Decision: Approved deferral of Email #1 screenshot asset while closing 7.13 infrastructure scope.
- Guardrail: Email #1 cannot be finalized/sent externally in task 7.14 without this asset.

---

## Re-run Command (Post-Brevo Verification)

```bash
python cis-discord-bot/scripts/task-713-brevo-audit.py --env-file cis-discord-bot/.env --sender-email trevor@k2mlabs.com --domain k2mlabs.com --send-test --test-to k2m.labs@gmail.com --wait-seconds 45 --output _bmad-output/cohort-design-artifacts/operations/sprint/task-notes/7.13-brevo-audit-k2mlabs.json
```
