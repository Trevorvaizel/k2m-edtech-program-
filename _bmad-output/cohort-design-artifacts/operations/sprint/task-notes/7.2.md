# Task 7.2 — Unique per-student Discord invite links (on_member_join match key)

**Completed:** 2026-03-05
**Decisions implemented:** B-01, GAP FIX #1, GAP FIX #8, N-07
**Status:** DONE — 13/13 tests passing

---

## What was built

### 1. `/api/interest` — real Discord invite creation
**File:** `cis-discord-bot/cis_controller/interest_api_server.py`

- `create_discord_invite_link()` (lines 55–104) already existed — uses Discord REST API with `max_uses=1, unique=True`, requires `DISCORD_TOKEN` + `DISCORD_INVITE_CHANNEL_ID` env vars.
- `_handle_interest()` updated:
  - Calls `create_discord_invite_link()` for non-waitlisted students
  - Falls back to `_generate_placeholder_invite_code(email)` if Discord API fails (and `DISCORD_INVITE_FALLBACK_URL` is set)
  - If neither works: returns HTTP 503 (refuses to silently lose the invite linkage)
  - Real `invite_code` (e.g. `"AbCdEf"`) written to Google Sheets Column R
  - `invite_link` (e.g. `"https://discord.gg/AbCdEf"`) embedded in Email #1 as a clickable anchor
- `send_brevo_email()` updated: accepts `invite_link` param; Email #1 now contains the personalised invite URL. Returns `False` if `invite_link` is absent for non-waitlisted sends.

### 2. `on_member_join` — invite diff + asyncio.Lock
**File:** `cis-discord-bot/main.py`

- `import asyncio` added at top.
- Module-level vars added:
  ```python
  _guild_invite_snapshot: dict = {}   # guild_id → {code: uses}
  _member_join_lock = asyncio.Lock()  # Decision N-07
  ```
- `on_ready` now snapshots all guild invites on startup:
  ```python
  for guild in bot.guilds:
      invites = await guild.invites()
      _guild_invite_snapshot[guild.id] = {inv.code: inv.uses for inv in invites}
  ```
- `on_member_join` fully rewritten:
  - Wrapped in `async with _member_join_lock` (Decision N-07 — prevents race on concurrent joins)
  - Fetches current guild invites post-join
  - Diffs against snapshot to find `used_code`:
    - Case A: invite still exists, `uses` incremented → that's the one
    - Case B: invite disappeared (max_uses=1 consumed and deleted by Discord) → that's the one
  - Updates snapshot after each join
  - Queries PostgreSQL: `get_student_by_invite_code(used_code)`
  - On match: calls `link_student_by_invite()` → writes `discord_id` + `discord_username` to student row
  - **Role:** assigns `@Guest` on join (NOT `@Student` — Guardrail #8; @Student is granted post-payment)
  - **Observability:** `log_observability_event("student_linked", ...)` or `"student_unmatched"`
  - **DM:** personalised DM if matched (uses `enrollment_name`), generic redirect DM if unmatched
  - Handles `discord.Forbidden` for both invite fetch and DM send

### 3. `/recover-member` slash command
**File:** `cis-discord-bot/main.py` (lines ~789–875)

- Slash command: `/recover-member member:<@User> invite_code:<optional>`
- With `invite_code`: exact match via `get_student_by_invite_code()`
- Without `invite_code`: temporal fallback via `get_recent_unlinked_student(hours=24)` — finds the most recently created unlinked student (Decision GAP FIX #8)
- Calls `link_student_by_invite()` and assigns `@Guest` if not already present
- Trevor-only by convention (slash commands scope)

### 4. `pg_store.py` — temporal fallback method added
**File:** `cis-discord-bot/database/pg_store.py`

```python
def get_recent_unlinked_student(self, hours: int = 24):
    """Returns most recent student with __pending__ discord_id enrolled within N hours."""
```

### 5. Database
**Status: already done by Task 7.6**
- `invite_code VARCHAR(20)` column: in `schema_pg.sql` table definition (line 27) + idempotent `ALTER TABLE ADD COLUMN IF NOT EXISTS` migration (line 394)
- `CREATE INDEX IF NOT EXISTS idx_students_invite ON students(invite_code)` (line 69)
- `get_student_by_invite_code()` and `link_student_by_invite()` already in `pg_store.py` from Task 7.6

---

## Tests
**File:** `cis-discord-bot/tests/test_task_72_invite_matching.py`
**Result:** 13/13 PASSED

| Test | Covers |
|------|--------|
| `test_create_discord_invite_link_success` | Discord API 201 → returns code + url |
| `test_create_discord_invite_link_missing_env` | Missing env vars → returns None |
| `test_create_discord_invite_link_api_error` | 403 response → returns None |
| `test_get_student_by_invite_code_match` | pg_store lookup hit |
| `test_get_student_by_invite_code_no_match` | pg_store lookup miss → None |
| `test_link_student_by_invite_success` | UPDATE + verify SELECT → True |
| `test_link_student_by_invite_already_claimed` | No row updated → False |
| `test_invite_diff_incremented_uses` | Uses 0→1 detected |
| `test_invite_diff_deleted_invite` | Invite disappears from map detected |
| `test_invite_diff_no_change` | No change → None |
| `test_invite_diff_empty_snapshot` | Empty old map → detects increment |
| `test_handle_interest_stores_real_invite_code` | End-to-end: real code stored in Column R |
| `test_handle_interest_falls_back_on_invite_failure` | Fallback URL used on API failure |

---

## Env vars required (add to Railway)

| Var | Required | Description |
|-----|----------|-------------|
| `DISCORD_INVITE_CHANNEL_ID` | **Yes** | Channel ID where invites are created (welcome/general) |
| `DISCORD_INVITE_FALLBACK_URL` | Optional | Static invite URL if API fails (set to a permanent server invite) |

---

## Known gaps / next
- Task 7.3: KIRA DM architecture audit — ensure KIRA DMs are gated on `discord_id` being set (B-03)
- The `link_student_by_invite` UPDATE uses `discord_id IS NULL OR discord_id = '' OR discord_id LIKE '__pending__%'` condition — prevents accidental re-linking if student re-joins
- If multiple students enrolled in last 24h without invite codes: `/recover-member` picks the most recent; Trevor should verify manually before confirming
