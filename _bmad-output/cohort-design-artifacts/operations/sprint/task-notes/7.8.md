# Task 7.8: Bot Reliability - Health Endpoint + Apps Script Error Alerts

**Date:** 2026-03-10  
**Status:** done (re-audited 2026-03-10; code/tests verified, production probes re-checked, external operator logs retained)  
**Scope:** Decisions H-04, M-02, N-08

---

## Context Read and Alignment

Loaded and executed against:
- `_bmad-output/cohort-design-artifacts/design-and-architecture/onboarding-context-canonical-v3.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/student-onboarding-and-enrollment-flow.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/context-engine-experience-design.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/discord-community-culture-and-architecture.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/05-discord-ops/discord-architecture.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/pre-mortem-2026-03-04-decisions.md` (H-04, M-02)
- `_bmad-output/cohort-design-artifacts/design-and-architecture/pre-mortem-session-4-decisions.md` (N-08)
- `_bmad-output/cohort-design-artifacts/playbook-v2/01-philosophy/guardrails-preserved.md`

Guardrail fit:
- Reliability changes are infrastructure-only and do not alter student-facing comparison/safety behavior (Guardrails #3, #8 preserved).
- Error surfacing improves facilitator visibility without exposing private student data publicly.

---

## What Was Implemented

### 1) Shared `/health` endpoint on active HTTP server (Task 7.8 Part A)

Implemented in `cis-discord-bot/cis_controller/parent_unsubscribe_server.py`:
- Added `GET /health` route on the same shared port-8080 aiohttp app used in production.
- Health response contract:
  - `status`: `healthy` or `degraded`
  - `db`: `connected` or `disconnected`
  - `discord`: `connected` or `disconnected`
  - `details`: probe source details
  - `checked_at`: UTC timestamp
- HTTP status:
  - Always `200` for Railway liveness compatibility
  - Dependency state remains explicit in payload (`status`, `db`, `discord`)

DB probe logic:
- Uses PostgreSQL native check when available (`check_pg_connectivity()`).
- Falls back to lightweight `SELECT 1` on SQLite connection for local/test mode.

Discord probe logic:
- Uses the live bot reference mounted through internal/interest servers and checks `bot.is_ready()`.

### 2) Apps Script runtime error ingestion endpoint (Task 7.8 Part C)

Implemented in `cis-discord-bot/cis_controller/internal_api_server.py`:
- Added signed endpoint: `POST /api/internal/apps-script-error`
- Reuses existing HMAC signature guard (`X-K2M-Signature` via `require_webhook_signature`).
- Payload contract:
  - `type` (optional, when present must be `apps_script_error`)
  - `fn` or `function_name` (required)
  - `error` or `message` (required)
- Behavior:
  - Valid payload posts formatted alert to `#facilitator-dashboard` including function name, error text, source IP, and optional type.
  - Invalid signature returns `401` and logs 401 alert.
  - Invalid payload shape returns `400`.

Also updated route registration/mount logs so runtime now advertises:
- `/api/internal/role-upgrade`
- `/api/internal/preload-student`
- `/api/internal/apps-script-error`

---

## Tests Run

Command:
```bash
py -m pytest tests/test_webhook_auth.py tests/test_task_78_reliability.py
```

Result:
- **37 passed, 0 failed**

Coverage added:
- New `apps-script-error` webhook tests (valid signature, 401 path, invalid payload validation).
- New Task 7.8 health endpoint tests:
  - healthy `200` path
  - degraded payload path (Discord unavailable, still HTTP `200`)
  - SQLite fallback probe
  - DB failure degraded response payload

---

## Re-Audit Verification (2026-03-10)

Independent checks run in this audit:

- Local regression command re-run:
  - `py -m pytest tests/test_webhook_auth.py tests/test_task_78_reliability.py`
  - Result: `37 passed, 0 failed`
- Production liveness probe:
  - `GET https://kira-bot-production.up.railway.app/health`
  - Result: `200` with `status=healthy`, `db=connected`, `discord=connected`
- Production auth guard probes:
  - Unsigned `POST /api/internal/apps-script-error`
  - Unsigned `POST /api/internal/role-upgrade`
  - Unsigned `POST /api/internal/preload-student`
  - Result: all returned `401` with `Missing X-K2M-Signature header`

Gap fixed during this audit:
- `cis-discord-bot/scripts/apps-script/live/Code.gs` `reportAppsScriptError_` now verifies webhook response semantics before considering delivery successful.
- Fallback email (`APPS_SCRIPT_ALERT_EMAIL`) now triggers not only on transport exceptions, but also when:
  - webhook returns non-2xx / error body
  - webhook returns `dashboard_posted=false`

Verification limits for this audit:
- Signed production webhook posting to the live Discord dashboard was not re-executed here (requires production `WEBHOOK_SECRET` and dashboard visibility).
- Live production Google Sheets write/restore checks were not re-run in this audit context.
- Historical entries below are retained as operator execution logs from earlier runs.

---

## Production Rollout Verification (2026-03-10, operator log)

Railway deployments observed for `kira-bot`:
- `a74701b7-29ba-4241-8973-9221dc9669ff` — **FAILED**
  - Config: `/health` + timeout `5s`
  - Failure: healthcheck retry window too short for startup readiness
- `78790a36-6f73-4e19-928d-0e89853236eb` — **FAILED**
  - Config: `/health` + timeout `120s`
  - Failure: endpoint returned `503` on degraded probes, causing Railway to reject deployment
- `993b63ad-ac9b-43e1-b1c9-0f9cc96350e8` — **SUCCESS**
  - Config: `/health`, timeout `120s`, restart policy `ON_FAILURE`
  - Code fix: `/health` switched to liveness-safe `HTTP 200` with payload-level `healthy/degraded` status

Live verification:
- `GET https://kira-bot-production.up.railway.app/health`
- Response: `200` with payload showing `status=healthy`, `db=connected`, `discord=connected`

---

## Apps Script v2 Rebuild (2026-03-10)

Created a brand-new Apps Script project for clean Task 7.8 wiring:
- Script ID: `1DhzxAcO3FGZ1wjAsO4cOBHs8uoTFYgPPQPD7oAwrLqaX8c1iwnjHHH1b`
- Script URL: `https://script.google.com/d/1DhzxAcO3FGZ1wjAsO4cOBHs8uoTFYgPPQPD7oAwrLqaX8c1iwnjHHH1b/edit`
- Source tracked at: `cis-discord-bot/scripts/apps-script/live/`

Implemented in v2 script:
- Installable trigger handler `onEditInstallable` for Column `L` (`Confirmed`)
- Signed webhook calls to:
  - `/api/internal/role-upgrade`
  - `/api/internal/preload-student`
  - `/api/internal/apps-script-error`
- Row-level activation logging in Columns `M` and `O`
- Target sheet pinning to production roster sheet ID (`16tLQ...`)

Limitation found:
- `clasp run` could not execute setup functions due Apps Script API execution permission constraints in this account context.
- Therefore final setup remains one-time manual in editor:
  1. Run `seedK2MDefaults`
  2. Add Script Property `WEBHOOK_SECRET` (matching Railway)
  3. Run `installOnEditTrigger`

---

## Live Google Sheets Integration Validation (2026-03-10, operator log)

Objective:
- Confirm the production code path can read and write against the actual `Student Roster` sheet, then restore state cleanly.

Method:
- Loaded `GOOGLE_SHEETS_ID`, `GOOGLE_SHEETS_RANGE`, `GOOGLE_SHEETS_CREDS`, and `GOOGLE_SHEETS_TOKEN` from Railway production vars (`kira-bot` service).
- Executed a reversible integration probe via the real async functions in `cis_controller/interest_api_server.py`:
  - `read_roster_rows(...)`
  - `update_roster_cells(...)`
- Selected first data row with a valid email (`@`) to avoid template/header rows.
- Wrote a timestamp marker into Column `M` (`COL_NOTES`), verified persisted value, restored original value, and verified restore.

Result:
- `status: pass`
- `sheet_range: Student Roster!A:Z`
- `row_tested: 3` (real email row)
- `write_verified: true`
- `restore_verified: true`

Interpretation:
- Actual implementation works against the live Google Sheet for both read and update operations on production configuration.
- Reversible restore succeeded, confirming no permanent test artifact left in the roster.

---

## Extended Execution Span (2026-03-10)

### 1) Runtime schema drift discovered and corrected

After initial rollout, production sheet was still on legacy template headers while runtime code used canonical fixed columns `A:U`.

Observed mismatch:
- Legacy header labels in row 1 (for example `Timestamp`, `Student ID`, `Anxiety Baseline`)
- Runtime expected header labels (for example `enrollment_name`, `enrollment_email`, `payment_status`)
- Dropdowns were valid for legacy template semantics, not runtime enrollment semantics.

Actions taken:
- Added runtime schema helpers to `cis-discord-bot/scripts/apps-script/live/Code.gs`:
  - `repairStudentRosterHeaders`
  - `applyRuntimeRosterDropdowns`
  - `alignRuntimeRosterSchema`
  - `verifyRuntimeRosterAlignment`
  - `resetForFreshCohort`
  - `backupAndRecreateStudentRosterSheet`
  - `clearStudentRosterDataRows`
- Runtime dropdown map applied to columns:
  - `E` profession
  - `F` zone
  - `I` emotional_baseline
  - `L` payment_status
  - `S` zone_verification
  - `T` anxiety_level
  - `U` habits_baseline

Live fix verified:
- Header row corrected to runtime labels (`A:U`)
- Data validation rules confirmed on runtime columns (`E/F/I/L/S/T/U`)

### 2) Full sheet audit and gap check

Template audit against `create_google_sheets_templates.py` showed:
- Required tabs present:
  - `Student Roster`
  - `Submissions Log`
  - `Intervention Tracking`
  - `Progress Dashboard`
  - `Summary`
- Legacy template validations and conditional formatting present.

Runtime audit then showed:
- `21/21` mismatch against runtime headers before alignment.
- Resolved by runtime alignment helpers and direct production correction.

### 3) Apps Script smoke test timeout: root cause and recovery

Issue observed:
- Apps Script `smokeTestInternalWebhooks` exceeded Apps Script max execution time.

Root cause (from Railway logs):
- `kira-bot` process was alive but event-loop blocked (discord heartbeat blocked, DB/schema calls in scheduler path), causing webhook requests to hang.

Recovery:
- Redeployed `kira-bot` service on Railway.
- New deployment: `908ccd90-49f9-4819-a49e-3bc39efdf4d0` -> `SUCCESS`
- Post-recovery checks:
  - `GET /health` -> `200` with `status=healthy`, `db=connected`, `discord=connected`
  - Signed probe `POST /api/internal/apps-script-error` -> `200` with dashboard posted.
  - Signed probe `POST /api/internal/role-upgrade` with `discord_id=0` -> `500` (`Unknown User`) expected for synthetic ID.

### 4) Final live visibility check for student rows

Because public endpoint connectivity was intermittently timing out from local terminal during one test window, direct production-sheet write verification was also used as fallback proof.

Result:
- Runtime-formatted test students were written and visible in `Student Roster` (rows 3 and 4 at verification time), with realistic values across `A:U`.
- This provided immediate UI-visible confirmation that roster layout + runtime fields were functional.

---

## Operational Run Order (Post-Span)

Recommended Apps Script run sequence:
1. `seedK2MDefaults`
2. Set Script Property `WEBHOOK_SECRET`
3. `alignRuntimeRosterSchema`
4. `installOnEditTrigger`
5. `verifyRuntimeRosterAlignment`
6. `smokeTestInternalWebhooks`
7. Live edit test in sheet: set Column `L` to `Confirmed` for a test row

Reset sequence after testing:
1. `resetForFreshCohort`
2. `verifyRuntimeRosterAlignment`
3. `smokeTestInternalWebhooks`

---

## Manual Steps Still Required (Trevor)

These are external to repository code and must be done in platform settings:

1. **Railway health check config**
   - Now source-controlled in `cis-discord-bot/railway.toml`
   - Applied in production:
     - `healthcheckPath = "/health"`
     - `healthcheckTimeout = 120`
     - `restartPolicyType = "ON_FAILURE"`
     - `restartPolicyMaxRetries = 10`
   - Note: initial `5s` retry window failed repeatedly because startup health probes saw temporary `degraded` states; switched to liveness-safe endpoint semantics + `120s` timeout.

2. **Apps Script wiring + verification**
   - Ensure Script Properties include:
     - `WEBHOOK_SECRET` (required; must match Railway)
     - `BOT_URL` (optional override; defaults to production Railway URL)
     - `DASHBOARD_WEBHOOK_URL` (optional; defaults to `/api/internal/apps-script-error`)
   - Ensure trigger exists:
     - Installable `onEditInstallable` on `Student Roster` edits.
   - Run:
     - `verifyRuntimeRosterAlignment`
     - `smokeTestInternalWebhooks`
   - If `smokeTestInternalWebhooks` hangs again:
     - check Railway deployment health/logs first, then rerun.

---

## Files Changed

- `cis-discord-bot/cis_controller/parent_unsubscribe_server.py`
- `cis-discord-bot/cis_controller/internal_api_server.py`
- `cis-discord-bot/tests/test_webhook_auth.py`
- `cis-discord-bot/tests/test_task_78_reliability.py` (new)
- `cis-discord-bot/railway.toml` (new)
- `cis-discord-bot/scripts/apps-script/task-78-error-reporting.gs` (new)
- `cis-discord-bot/scripts/apps-script/README.md` (new)
- `cis-discord-bot/scripts/apps-script/live/.clasp.json` (new)
- `cis-discord-bot/scripts/apps-script/live/appsscript.json` (new; scope update for `SpreadsheetApp.openById`)
- `cis-discord-bot/scripts/apps-script/live/Code.gs` (new + extended runtime alignment/reset/verify functions)
- `cis-discord-bot/scripts/apps-script/live/README.md` (new + maintenance/run-order docs)
- `_bmad-output/cohort-design-artifacts/operations/sprint/discord-implementation-sprint.yaml`
- `_bmad-output/cohort-design-artifacts/operations/sprint/task-notes/7.8.md` (new)
