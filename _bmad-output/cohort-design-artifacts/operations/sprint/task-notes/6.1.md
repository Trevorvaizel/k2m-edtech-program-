# Task 6.1: Student Pre-Load Bridge + `on_member_join` Discord Linker

**Date:** 2026-03-12  
**Status:** done  
**Scope:** Sprint 6 task 6.1 rebased to Canonical V3 (`Sheets -> PostgreSQL`, invite-code linking)

---

## Discussion-First Alignment (Winston + Amelia)

Pre-execution decision:

- Treat `6.1` as a rebase-and-verify task (not greenfield), because Sprint 7 already delivered the critical mechanics (`7.2` invite linkage + `7.6` PostgreSQL migration).
- Validate against canonical guardrails/docs before marking complete.

---

## Context + Guardrail References Used

- `_bmad-output/cohort-design-artifacts/design-and-architecture/onboarding-context-canonical-v3.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/student-onboarding-and-enrollment-flow.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/context-engine-experience-design.md`
- `_bmad-output/cohort-design-artifacts/design-and-architecture/discord-community-culture-and-architecture.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/05-discord-ops/discord-architecture.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/01-philosophy/guardrails-preserved.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/01-philosophy/four-habits-brand.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/01-philosophy/node-architecture.md`
- `_bmad-output/cohort-design-artifacts/playbook-v2/01-philosophy/jtbd-integration.md`
- `_bmad-output/cohort-design-artifacts/operations/sprint/discord-implementation-sprint.yaml`

---

## What Was Verified

### A) PostgreSQL pre-load bridge (`preload_students.py`)

Verified in `cis-discord-bot/preload_students.py`:

- Approved-row filtering and roster extraction logic exists.
- Idempotent upsert behavior exists (safe reruns).
- Uses PostgreSQL/runtime fields including `invite_code` linkage context.

### B) `on_member_join` identity linker

Verified in `cis-discord-bot/main.py`:

- Invite-code diff linkage in `on_member_join` is implemented.
- Member remains gated (`@Guest`) before payment confirmation path upgrades access.
- Observability events `student_linked` / `student_unmatched` are emitted.

### C) Runtime store methods for invite linkage

Verified in `cis-discord-bot/database/pg_store.py`:

- `get_student_by_invite_code(...)`
- `link_student_by_invite(...)`

---

## Acceptance Mapping

- [x] Pre-load bridge exists and is PostgreSQL-aligned.
- [x] Idempotent sync path validated by targeted test suite.
- [x] `on_member_join` captures/links Discord identity by invite.
- [x] `@Student` is not granted pre-payment.
- [x] `student_linked` / `student_unmatched` observability path present.

---

## Test Evidence

Command:

```powershell
pytest cis-discord-bot/tests/test_task_72_invite_matching.py cis-discord-bot/tests/test_task_76_pg_completion.py -q
```

Result: `26 passed`

Evidence file:

- `_bmad-output/cohort-design-artifacts/operations/sprint/task-notes/6.1-pytest-invite-preload.txt`

---

## Rebase Note

The sprint YAML legacy SQLite wording for 6.1 was updated to PostgreSQL terminology per Canonical V3 precedence.
